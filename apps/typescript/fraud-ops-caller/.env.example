# Demo needs no keys. Live needs all three plus the desk confirm phrase
# "I understand this places a real phone call".
# Do not commit real credentials.
CALLE_API_KEY=
CALLE_BASE_URL=
CALLE_LIVE_CALLS_ENABLED=false
